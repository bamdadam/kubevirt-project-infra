listen to github events => pull request
* check event content
* how are test files represented

check changed file

if certain test files have changed => add a label to the 
* how to apply a label

apply the label
